name: Startup Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  startup:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Use Node 22.22.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.22.0
          cache: npm

      - name: Install exactly from lockfile
        run: npm ci --include=dev --no-audit --no-fund

      - name: Import appeal modules
        run: |
          node -e "require('ts-node').register({transpileOnly:true,project:'tsconfig.json'}); require('./src/commands/infractionAppeal.ts'); require('./src/commands/infractionAppealRecovery.ts'); require('./src/events/ticketAppealNaming.ts'); console.log('appeal modules ok')"

      - name: Strict TypeScript check
        run: npm run typecheck
