const test=require('node:test'),assert=require('node:assert/strict');const store=require('../src/store');let row=null;store.collection=()=>({findOne:async()=>row,replaceOne:async(_q,v)=>{row=v;},updateOne:async(_q,u)=>{if(row)Object.assign(row,u.$set);}});store.locked=async(_k,work)=>work();const {parse,request,USER_IDS}=require('../src/self-dm');
test('self-DM command accepts both users and caps at 50',()=>{assert.deepEqual(USER_IDS,['1066264414359138304','1262469782897295461']);assert.deepEqual(parse('hello 50'),{text:'hello',count:50});assert.throws(()=>parse('hello 51'),/1–50/);});
test('only the two specified accounts can start a job',async()=>{const client={users:{fetch:async id=>({id,send:async()=>{}})}};assert.match(await request({id:USER_IDS[1]},'hello 1',client),/both authorized/);await assert.rejects(request({id:'other'},'hello 1',client),/Only accounts/);await request({id:USER_IDS[0]},'stop',client);});
